/**
 * Shared by the desktop NavBar and the mobile menu, so the two can never drift apart.
 * Each href points at an `id` on a section in PageContent.
 */
export const sectionLinks = [
    { label: 'About', href: '#about' },
    { label: 'Experience', href: '#experience' },
    { label: 'Work', href: '#work' },
    { label: 'AI', href: '#ai' },
    { label: 'Contact', href: '#contact' },
];
