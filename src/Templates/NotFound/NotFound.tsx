import { NotFoundContainer, TextContainer } from './NotFound.style';
import { Title } from '../Connections/Connections.style';

const NotFound = () => {
    return (
        <NotFoundContainer>
            <TextContainer>
                <Title>Not Found</Title>
            </TextContainer>
        </NotFoundContainer>
    );
};

export default NotFound;
